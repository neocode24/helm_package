apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: simple02
spec:
  hosts:
          #  - simple02-web
  - "*"
          #  - "*"
          #  - simple02-package.container.ipc.kt.com
  gateways:
  - simple02-gateway
  http:
  - match:
    - port: 80
    route:
    - destination:
        host: simple02-web
        #        subset: v2
        #  - route:
        #    - destination:
        #        host: simple02-web
        #        subset: v1
